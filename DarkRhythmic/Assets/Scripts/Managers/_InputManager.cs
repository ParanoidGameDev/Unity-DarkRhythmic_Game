using UnityEngine;

public class _InputManager
{
    /*public bool escape;

    private void Update() {
        // ! KEYBOARD ==

        //Escape key
        if (Input.GetKeyDown(KeyCode.Escape)) escape = true;
        else escape = false;
    }
    */
}
